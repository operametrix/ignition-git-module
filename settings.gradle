pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = "https://nexus.inductiveautomation.com/repository/public"
        }
    }
}

rootProject.name = "ignition-git-module"

dependencyResolutionManagement {
    repositories {
        maven {
            url "https://nexus.inductiveautomation.com/repository/public"
        }
        mavenCentral()
    }
}

include(
    ":",
    ":client",
    ":common",
    ":designer",
    ":gateway"
)
