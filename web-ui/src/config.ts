// Data-route base for the module's gateway REST handlers (mounted via getMountPathAlias = "git-config").
// NOTE: if routes resolve under a different prefix on a live gateway (e.g. /main/data/git-config),
// adjust this single constant.
export const BASE = "/data/git-config";
